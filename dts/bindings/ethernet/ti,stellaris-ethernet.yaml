---
title: TI Stellaris ETH
id: ti,stellaris-ethernet
version: 0.1

description: >
    This binding gives a base representation of the TI Stellaris ETHERNET

properties:
    compatible:
      type: string
      category: required
      description: compatible strings
      constraint: "ti,stellaris-ethernet"

    reg:
      type: array
      description: mmio register space
      generation: define
      category: required

    interrupts:
      type: array
      category: required
      description: required interrupts
      generation: define

    mac_addr:
      type: array
      category: required
      description: mac address
      generation: define

    drv_name:
      type: string
      category: required
      description: Driver name
      generation: define
...
